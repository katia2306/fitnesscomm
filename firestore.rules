service cloud.firestore {
  match /databases/{database}/documents {

    match /users/{userId} {
      allow read, update: if request.auth.uid == userId;
    }

    match /users/{userId}/profiles/{profile} {
      allow read, write: if request.auth.uid == userId;
    }
  }
}